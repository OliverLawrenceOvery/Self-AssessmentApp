<UserControl x:Class="SelfAssessmentService_WPF.Navigation.NavigationBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SelfAssessmentService_WPF.Navigation"
             xmlns:nav="clr-namespace:SelfAssessmentService_WPF.State.Navigator"
             xmlns:vm="clr-namespace:SelfAssessmentService_WPF.ViewModels"
             xmlns:converters="clr-namespace:SelfAssessmentService_WPF.Converters"
             
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:EqualParameterValue x:Key="EqualValue"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.Resources>
                <Style TargetType="RadioButton" BasedOn="{StaticResource NavigationButton}"/>
            </Grid.Resources>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>

            <RadioButton Grid.Column="0" Content="Home" Command="{Binding UpdateCurrentViewCommand}" CommandParameter="{x:Static nav:ViewType.Home}" />
            <RadioButton Grid.Column="1" Content="Profile" Command="{Binding UpdateCurrentViewCommand}" CommandParameter="{x:Static nav:ViewType.Profile}"/>
            <RadioButton Grid.Column="2" Content="Resources"/>
            <RadioButton Grid.Column="3" Content="Tests"/>
        </Grid>
    </Grid>
</UserControl>
